# BUIR Studio OS Desktop Shell

Minimal Tauri 2 desktop shell that loads the production website at
`https://os.buirpoint.top`.

## Development

```powershell
pnpm install --ignore-workspace
pnpm dev
```

This phase intentionally provides no Tauri commands, plugins, tray integration,
notifications, autostart, local file access, or offline cache.
