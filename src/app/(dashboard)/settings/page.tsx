import { GearSix, ShieldCheck } from "@phosphor-icons/react/dist/ssr";
import { PageIntro } from "@/components/layout/page-intro";
import { requireProfile } from "@/lib/auth/guards";
import { roleLabels } from "@/lib/auth/roles";

export default async function SettingsPage() { const { profile } = await requireProfile(); return <div className="mx-auto max-w-[1360px]"><PageIntro eyebrow="设置" title="账号与访问基础。" description="账号由工作室负责人在认证服务中维护。此页面暂不提供自助编辑。" /><section className="grid gap-4 lg:grid-cols-2"><div className="rounded-2xl border border-[var(--border)] bg-[var(--background)] p-6"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[var(--secondary)] text-[var(--primary)]"><GearSix size={21} /></span><div><h2 className="font-semibold">当前账号</h2><p className="text-sm text-[var(--muted-foreground)]">你的登录身份与角色</p></div></div><dl className="mt-7 grid gap-5"><div><dt className="text-xs text-[var(--muted-foreground)]">姓名</dt><dd className="mt-1 font-medium">{profile.name ?? "未设置"}</dd></div><div><dt className="text-xs text-[var(--muted-foreground)]">邮箱</dt><dd className="mt-1 font-medium">{profile.email}</dd></div><div><dt className="text-xs text-[var(--muted-foreground)]">角色</dt><dd className="mt-1 font-medium">{roleLabels[profile.role]}</dd></div></dl></div><div className="rounded-2xl bg-[var(--secondary)] p-6"><span className="grid size-10 place-items-center rounded-xl bg-[var(--background)] text-[var(--primary)]"><ShieldCheck size={21} /></span><h2 className="mt-5 font-semibold">访问控制已启用</h2><p className="mt-2 max-w-md text-sm leading-6 text-[var(--muted-foreground)]">页面路由由服务端会话与 profile 状态共同保护。导航隐藏不是唯一的权限机制。</p></div></section></div>; }
